# GUI front-end dependencies (optional).
#
# CLI users don't need these — the core runtime (requirements.txt) stays lean.
# Install on top of requirements.txt to run `python -m gui`.
#
# qasync is listed for completeness, but the GUI currently runs lcu-driver on a
# background thread (it owns/closes its own asyncio loop and can't cleanly share
# qasync's loop — see gui/bridge.py). qasync remains useful if a future lcu-driver
# exposes an awaitable start; remove it if you never adopt that path.
-r requirements.txt
PySide6>=6.6
qasync>=0.27
